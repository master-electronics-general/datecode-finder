command: ["streamlit", "run", "app.py", "--server.port=8080", "--server.address=0.0.0.0"]

resources:
  - name: sql_warehouse
    sql_warehouse:
      id: "e81dde068949155d"
      permission: "CAN_USE"
